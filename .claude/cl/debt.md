# Technical Debt

- **CSP unsafe-inline**: `'unsafe-inline'` was added to scriptSrc in helmet config to allow the injected `window.__NARRATORR_URL_BASE__` config script. Consider using a nonce-based approach instead for stricter CSP. (discovered in #284)
- **registerStaticAndSpa untested**: The HTML injection and SPA fallback logic in `server-utils.ts` isn't unit tested because it requires a real filesystem with `dist/client/index.html`. An integration test with a temp directory would be more robust. (discovered in #284)
- **QualityGateService max-lines override**: File grew to 580 lines after adding `getQualityGateDataBatch()`. The SSE emission calls are mechanical boilerplate — consider extracting a helper. (discovered in #283, worsened in #356)
- **Activity/event-history routes still use string-matching error handling**: Routes match error messages via `message.includes('not found')` instead of typed error classes. (discovered in #359)
- **No backup encryption**: Backups contain the raw SQLite DB including hashed passwords and API keys. Acceptable for single-user self-hosted, but needs encryption if multi-user is ever added. (discovered in #280)
- **GeneralSettings probeFfmpeg untested during form submission**: No test covers the mutation path or error case where `probeFfmpeg` rejects. (discovered in #332)
- **Pending restore state is in-memory only**: The `pendingRestore` object with 5-min TTL lives in BackupService memory. Low risk but a file-based marker would be more resilient. (discovered in #280)
- **CredentialsSection lacks dedicated test file**: Tested indirectly through SecuritySettings integration tests only. (discovered in #358)
- **TaskRegistry.estimateNextRun() rough approximation**: Only handles simple `*/N` cron patterns. A proper cron parser would give accurate times. (discovered in #279)
- **CI quality-gates job duplicated**: `docker.yaml` copies the full quality-gates job from `ci.yaml`. Consider composite action if a third workflow appears. (discovered in #175)
- **RecyclingBinService concurrent restore race**: Two simultaneous restores could both pass conflict check. Low risk. (discovered in #331)
- **eslint-plugin-react-hooks blocks ESLint 10**: Peer dep only goes up to ESLint 9. Wait for stable release. (discovered in #329)
- **No key rotation CLI**: Changing encryption key requires manual re-encryption. A `key-rotate` CLI would automate it. (discovered in #315)
- **ImportListsSettings form labels lack htmlFor**: Labels without `htmlFor`/`id` pairing, inaccessible to screen readers. (discovered in #285)
- **Hardcoded processing fixtures in test files**: Multiple test files use full hardcoded processing objects instead of `createMockSettings()`. (discovered in #198)
- **runUpgradeSearchJob not deduplicated**: Still has its own search→filter→rank→grab loop that could be DRYed up. (discovered in #357)
- **jobs/search.ts re-exports from search-pipeline**: Backward compat re-exports that can be cleaned up when all consumers import directly. (discovered in #357)
- **BookService.getAll() slim select**: Explicit column list must be kept in sync with schema. Consider deriving from full definition minus excluded. (discovered in #355)
- **jobs/index.test.ts hardcoded job count**: `toHaveLength(12)` will break every time a new job is added. (discovered in #366)
- **createMockServices hardcoded service names**: Service list in `helpers.ts` must be manually updated. (discovered in #366)

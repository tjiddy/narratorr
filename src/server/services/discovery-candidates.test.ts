import { describe, it } from 'vitest';

describe('toScoredCandidate', () => {
  it.todo('maps book.authors[0]?.asin to authorAsin field');
  it.todo('sets authorAsin to undefined when author has no ASIN');
  it.todo('sets authorName to Unknown and authorAsin to undefined when book has no authors');
});

import { describe, it } from 'vitest';

describe('LibraryModals', () => {
  describe('delete modal file count label', () => {
    it.todo('shows "Also delete N files from disk" when deleteTarget has audioFileCount');
    it.todo('shows "Delete files from disk" when deleteTarget has no audioFileCount');
    it.todo('hides toggle when deleteTarget has no path');
  });
});

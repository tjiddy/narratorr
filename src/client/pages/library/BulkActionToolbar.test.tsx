import { describe, it } from 'vitest';

describe('BulkActionToolbar', () => {
  describe('file count in delete toggle', () => {
    it.todo('passes fileCount prop to DeleteBookModal');
    it.todo('shows file count label when fileCount > 0');
    it.todo('shows generic label when fileCount is 0');
    it.todo('hides toggle when hasPath is false');
  });
});

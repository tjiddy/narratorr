import { describe, it } from 'vitest';

describe('ImportQueuedRow', () => {
  it.todo('renders cover thumbnail, title, and primary author name');
  it.todo('displays QUEUED eyebrow label');
  it.todo('does not render phase rows or progress');
  it.todo('multiple rows stack with dividers in a glass-card container');
});

import { describe, it } from 'vitest';

describe('ManualAddFormModal', () => {
  describe('AC4 — aria-labelledby', () => {
    it.todo('dialog element has aria-labelledby attribute referencing the form heading');
    it.todo('heading text matches expected title');
  });
});

import { describe, it } from 'vitest';

describe('RouteErrorBoundary', () => {
  it.todo('renders children normally when no error');
  it.todo('catches render error and shows route-scoped fallback');
  it.todo('displays error message in the fallback');
  it.todo('resets error state when user clicks Try Again');
  it.todo('does not replace the Layout shell — only content area shows error');
});

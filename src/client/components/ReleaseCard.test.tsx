import { describe, it } from 'vitest';

describe('ReleaseCard', () => {
  describe('#324 — quality badge when duration unknown (verify only)', () => {
    it.todo('when bookDurationSeconds is null/0/undefined, no quality badge rendered');
    it.todo('when bookDurationSeconds is valid and result.size > 0, quality badge renders with tier and MB/hr');
  });
});

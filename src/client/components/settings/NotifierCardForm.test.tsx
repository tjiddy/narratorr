import { describe, it } from 'vitest';

describe('NotifierCardForm (#224)', () => {
  describe('SelectWithChevron migration', () => {
    it.todo('type select renders with SelectWithChevron (appearance-none and ChevronDownIcon visible)');
    it.todo('selecting a notifier type via SelectWithChevron updates form state');
  });
});

import { describe, it } from 'vitest';

// ============================================================================
// #257 — Merge observability: shared event-history schema
// ============================================================================

describe('#257 merge observability — event-history schema', () => {
  it.todo('eventTypeSchema includes merge_started and merge_failed');
  it.todo('eventTypeSchema still includes all original 11 event types');
  it.todo('rejects unknown event type strings');
});

import { describe, it } from 'vitest';

describe('CANONICAL_LANGUAGES', () => {
  it.todo('exports a non-empty readonly array of lowercase language names');
  it.todo('is alphabetically sorted');
  it.todo('includes english as a valid entry');
  it.todo('all entries are lowercase strings');
});

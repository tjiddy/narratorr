/** Maximum cover image file size: 10 MB. */
export const MAX_COVER_SIZE = 10 * 1024 * 1024;

/** Bytes in one gibibyte (1 GiB = 1024³). Used for size-threshold conversions. */
export const BYTES_PER_GB = 1_073_741_824;

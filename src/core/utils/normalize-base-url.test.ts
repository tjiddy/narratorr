import { describe, it } from 'vitest';

describe('normalizeBaseUrl', () => {
  it.todo('strips single trailing slash from URL');
  it.todo('strips multiple trailing slashes from URL');
  it.todo('returns URL unchanged when no trailing slash');
  it.todo('handles empty string without throwing');
  it.todo('returns undefined for undefined input (nullable passthrough)');
});
